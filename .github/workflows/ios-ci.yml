name: iOS CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test
    runs-on: macos-14
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Select Xcode Version
      run: sudo xcode-select -switch /Applications/Xcode_16.2.app/Contents/Developer
      
    - name: Show Xcode Version
      run: xcodebuild -version
      
    - name: Show Available Simulators
      run: xcrun simctl list devices available | grep -E "(iPhone|iPad)" | head -10
      
    - name: Cache derived data
      uses: actions/cache@v4
      with:
        path: ~/Library/Developer/Xcode/DerivedData
        key: ${{ runner.os }}-derived-data-${{ hashFiles('**/*.xcodeproj') }}
        restore-keys: |
          ${{ runner.os }}-derived-data-
    
    - name: Build for Testing
      run: |
        xcodebuild \
          -project "Randall iOS.xcodeproj" \
          -scheme "Randall iOS" \
          -destination "platform=iOS Simulator,name=iPhone 16" \
          -derivedDataPath ~/Library/Developer/Xcode/DerivedData \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          build-for-testing
    
    - name: Run Unit Tests
      run: |
        xcodebuild \
          -project "Randall iOS.xcodeproj" \
          -scheme "Randall iOS" \
          -destination "platform=iOS Simulator,name=iPhone 16" \
          -derivedDataPath ~/Library/Developer/Xcode/DerivedData \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          -enableCodeCoverage YES \
          test-without-building
    
    - name: Generate Code Coverage Report
      run: |
        xcrun xccov view --report --json ~/Library/Developer/Xcode/DerivedData/*/Logs/Test/*.xcresult > coverage.json
        
    - name: Upload Coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        files: ./coverage.json
        fail_ci_if_error: false
        verbose: true
        
  build-release:
    name: Build Release
    runs-on: macos-14
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Select Xcode Version
      run: sudo xcode-select -switch /Applications/Xcode_16.2.app/Contents/Developer
      
    - name: Cache derived data
      uses: actions/cache@v4
      with:
        path: ~/Library/Developer/Xcode/DerivedData
        key: ${{ runner.os }}-derived-data-release-${{ hashFiles('**/*.xcodeproj') }}
        restore-keys: |
          ${{ runner.os }}-derived-data-release-
          ${{ runner.os }}-derived-data-
    
    - name: Build Release
      run: |
        xcodebuild \
          -project "Randall iOS.xcodeproj" \
          -scheme "Randall iOS" \
          -destination "generic/platform=iOS" \
          -configuration Release \
          -derivedDataPath ~/Library/Developer/Xcode/DerivedData \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          IPHONEOS_DEPLOYMENT_TARGET=18.2 \
          build
          
    - name: Archive App
      run: |
        xcodebuild \
          -project "Randall iOS.xcodeproj" \
          -scheme "Randall iOS" \
          -destination "generic/platform=iOS" \
          -configuration Release \
          -derivedDataPath ~/Library/Developer/Xcode/DerivedData \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          IPHONEOS_DEPLOYMENT_TARGET=18.2 \
          archive \
          -archivePath ~/Library/Developer/Xcode/DerivedData/RandallApp.xcarchive